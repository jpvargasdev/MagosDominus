services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
    command: ["air", "-c", ".air.toml"]
    container_name: magos-dominus
    environment:
      ENV: "debug"

